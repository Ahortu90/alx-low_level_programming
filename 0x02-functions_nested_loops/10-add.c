#include "main.h"
#include <stdio.h>

/**
 * add - add two ints
 * @a: first int to add
 * @b: second int to add
 * Description: Function that adds two integers and returns the result
 * Return: sum of both ints
 */

int add(int a, int b)
{
	return (a + b);
}
